{
	"name": "portfolio",
	"type": "module",
	"version": "0.0.1",
	"scripts": {
		"dev": "astro dev",
		"build": "astro build",
		"preview": "astro preview",
		"astro": "astro",
		"deploy": "astro build && wrangler pages deploy",
		"cf-typegen": "wrangler types",
		"clean": "rm -rf dist .astro node_modules",
		"lint": "echo 'optional later'",
		"start": "pnpm dev",
		"deploy:cf": "pnpm build"
	},
	"dependencies": {
		"astro": "^5.17.1"
	},
	"devDependencies": {
		"@types/node": "^25.3.0"
	}
}
